console.log("i am fyne")
 export const square=(x)=>x*x;
 export const add=(a,b)=>a+b;
 export default (a,b)=>a-b;
 //we can use direclty without varibale name or function for default exporting
 //export default subtract